import React from 'react';

const Loading = props => (
    // loading.io
    <section className="loading">
        <img src="../../assets/loading.gif" alt="Page Loading" title="Page Loading" />
    </section>
);

export default Loading; 